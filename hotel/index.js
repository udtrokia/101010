;(function() {
  /* Global Variables */
  const dom = document;
  function dqs(ident) { return dom.querySelector(ident) }
  function dce(ident) { return dom.createElement(ident) }
  
  /* Fixed DOM */
  const ut = dqs('#up-title');
  const dt = dqs('#down-title');
  const uc = dqs('#up-container');
  const dc = dqs('#down-container');
  
  /* Home Page */
  

  /* Detail Page */
  
})();
